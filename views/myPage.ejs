<!DOCTYPE html>
<html lang="ko">

<head>
  <link rel='stylesheet' href='/stylesheets/panPageStyle.css' />
  <% include ./head %>
</head>

<body>
  <% include ./header %>
  <div class="container">
    <div class="row">

    <div class="col-md-3">
      <br><br><br><br>
      <% if(me.sideJ == 1){ %>
      <p>성향 : 진보</p>
      <% }else{ %>
      <p>성향 : 보수</p>
      <% } %>
      <p>닉네임 : <%= me.nameJ %> </p>
      <p>출생연도 : <%= me.ageJ %> </p>
      <p>오늘 쓴 글 : <%= todayWrite %> 개</p>
      <p>레벨 : <%= me.level %> </p>
      <p>누적경험치 : <%= me.exp %> </p>
  </div>

    <div class="col-md-9">

      <hr>
    <div class="row">
      <div class="col-md-6 text-center">제목</div>
      <div class="col-md-1 text-center">인정</div>
      <div class="col-md-2 text-center">시간</div>
      <div class="col-md-3 text-center">권한</div>
    </div>
    <hr>
    <% if (panArr !== null){
    for(var i = 0; i < panArr.length; i++) { %>
    <div class="row" style="height:50px;">
      <div class="col-md-5">
        <button class="uk-button uk-button-text" onclick="location.href='/pan/<%= panArr[i]._id%>'">
          <%=panArr[i].title%>
        </button>
      </div>
      <div class="col-md-2 text-center">
        <%= panArr[i].like_number%>
      </div>
      <div class="col-md-5 text-center uk-button-group">
        <div class="uk-inline">
        <% if(panArr[i].exp_done==false){ %>
        <button class="uk-button uk-button-default" onclick='location.href="/user/expup/<%= panArr[i]._id %>"'>정산</button>
        <% }else{ %>
        <span>정산완료 :
          <%= panArr[i].exp_done_howmuch %></span>
        <% } %>
        <button class="uk-button uk-button-default" onclick="location.href='/pan/update/<%= panArr[i]._id %>'">수정</button>
        <button class="uk-button uk-button-default" onclick="location.href='/pan/remove/<%= panArr[i]._id %>'">삭제</a></button>
      </div>
      </div>
    </div>
    <br>
    <%  }} %>
    <div class="text-center">
      <ul class="uk-pagination uk-flex-center">
        <% var start=page;
        if((start%10)==0){
          start--;
        }
        while((start%10)!=0){
          start--;
        }
        start++;
        if((start+9)>pagination){
          end=pagination;
        }else{
          end=start+9;
        }
        if(start!==1){ %>
        <li><a href="./<%=start-1%>"><span uk-pagination-previous></span>
          </a></li>
        <%}
      for(var i=start; i<=end; i++){ %>
        <li><a href="./<%=i%>" class="page-link">
            <%=i%></a></li>
        <%}%>
        <%if(end!=pagination){ %>
        <li><a href="./<%=end+1%>"> <span uk-pagination-next></span></a></li>
        <%} %>
      </ul>
    </div>
  </div>
  </div>
  </div>
  <div class="container">
    <hr>
    <% include ./footer %><br></div>
</body>

</html>
