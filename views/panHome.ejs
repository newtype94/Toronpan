<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="utf-8">
  <% include ./head %>
</head>

<body>
  <% include ./header %>
  <div class="container">
    <% if((message != null)&&(message != "")){ %>
    <div class="row mt-2 mb-2">
      <div class="col-md-12">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <strong>알림</strong>
          <%=message%>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      </div>
    </div>
    <% } %>

    <% if(survey != null){ %>
    <div class="row">
      <div class="col-xs-12">
        <div class="card border-info mt-2 mb-2" style="max-width: 20rem;">
          <div class="card-header text-center">오늘의 설문</div>
          <div class="card-body text-info">
            <h4 class="card-title">
              <%= survey.q %>
            </h4>
            <form action="/survey/do" method="post">
              <div class="radio">
                <label for="degree_1"><input type="radio" name="degree" value="1" id="degree_1" />못하고있다</label>
                <label for="degree_2"><input type="radio" name="degree" value="2" id="degree_2" />중간은 한다</label>
                <label for="degree_3"><input type="radio" name="degree" value="3" id="degree_3" />잘하고 있다</label>
              </div>
              <input type="hidden" name="firstID" value="<%= survey.id %>" />
              <button type="submit" class="btn btn-danger">오늘 제출완료</button>
            </form>
          </div>
        </div>
      </div>
    </div>
    <% }%>

    <div class="row mt-3 mb-3">
      <div class="col-12">
        <form class="form-inline" action="/pan/search/1" method="post">
          <select class="form-control" name="where">
            <option value="최신">최신</option>
            <option value="인기">인기</option>
          </select>
          <select class="form-control mr-sm-2" name="how">
            <option value="제목">제목</option>
            <option value="제목">내용</option>
            <option value="제목+내용">제목+내용</option>
          </select>
          <input class="form-control mr-sm-2" name="what" type="text" type="search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">검색</button>
        </form>
      </div>
    </div>

    <% if(login == 1){ %>
    <div class="row">
      <div class="col-12">
        <div class="card border-info mt-3 mb-3" style="max-width: 20rem;">
          <div class="card-header text-center">회원가입</div>
          <div class="card-body text-info">
            <h4 class="card-title">
              추후 수정이 불가하니, 정확하게 입력해주세요
            </h4>
            <form action="/joinDB" method="post">
              <div class="form-group">
                <label for="nameJ">활동하실 닉네임
                  <input type="text" class="form-control" name="nameJ" id="nameJ" /></label>
                <div id="checkMsg"></div>
              </div>
              <div class="form-group">
                <div class="radio">
                  <label for="m"><input type="radio" name="genderJ" value="male" id="m" />남 </label>
                  <label for="f"><input type="radio" name="genderJ" value="female" id="f" />여</label>
                </div>
              </div>
              <div class="form-group">
                <label for="ageJ">출생 연도
                  <input type="text" class="form-control" name="ageJ" id="ageJ" />
                </label>
              </div>
              <div class="form-group">
                <div class="radio">
                  <label for="left"><input type="radio" name="sideJ" value="left" id="left" />진보 </label>
                  <label for="right"><input type="radio" name="sideJ" value="right" id="right" />보수 </label>
                </div>
              </div>
              <button type="submit" class="btn btn-primary">가입</button>
            </form>
          </div>
        </div>
      </div>
    </div>
    <% }%>

  </div>
</body>

<script type="text/javascript">
  $(document).ready(function() {
    $("#nameJ")
      .keyup(function() {
        if ($('#nameJ').val() != "") {
          $.ajax({
            type: 'post',
            url: '/idcheck/' + $('#nameJ').val(),
            dataType: 'json',
            success: function(json) {
              var comment = $.parseJSON(json);
              if (comment.count == 0) {
                $('#checkMsg').html('<p class="text-primary">사용할 수 있습니다</p>');
              } else {
                $('#checkMsg').html('<p class="text-danger">이미 존재하는 닉네임입니다</p>');
              }
            }
          });
        }
      })
      .keyup();
  });
</script>

</html>
